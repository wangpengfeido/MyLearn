动态chunk中，引用的node_modules中模块，并符合其他条件，被分离

因为下面的默认配置：
````
    splitChunks: {
      chunks: 'async',
      cacheGroups:{
        defaultVendors: {
          test: /[\\/]node_modules[\\/]/,
          priority: -10
        },
      }
    }
````

