import { SyncRegValidatorDirective } from './sync-reg-validator.directive';

describe('SyncRegValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncRegValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
