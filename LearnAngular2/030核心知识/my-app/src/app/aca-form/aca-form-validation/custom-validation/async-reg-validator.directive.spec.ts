import { AsyncRegValidatorDirective } from './async-reg-validator.directive';

describe('AsyncRegValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncRegValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
